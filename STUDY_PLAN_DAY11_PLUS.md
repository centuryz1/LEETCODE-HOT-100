# 秋招冲刺计划：Day 11 - Day 26

制定时间：2026-09-23

上一阶段：Day 01 - Day 10 已完成，共 40 题。

本阶段目标：用 14 天主线（Day 11 - Day 24）覆盖剩余 60 题中的 35 道面试最高频题（P0），再用 2 天（Day 25 - Day 26）补重点题和选刷题。

---

## 一、进度对照

### 已完成 40 题（Day 01 - Day 10）

| 分类 | 已完成题目 |
| --- | --- |
| 哈希 | 两数之和、字母异位词分组、最长连续序列 |
| 双指针 | 移动零、盛最多水的容器、三数之和、接雨水 |
| 滑动窗口 | 无重复字符的最长子串、找到字符串中所有字母异位词 |
| 子串 | 和为 K 的子数组、滑动窗口最大值、最大子数组和 |
| 数组与矩阵 | 合并区间、轮转数组、除自身以外数组的乘积、螺旋矩阵、矩阵置零 |
| 链表 | 相交链表、反转链表、环形链表、回文链表、合并两个有序链表、两数相加、删除倒数第 N 个、环形链表 II |
| 二叉树 | 中序遍历、最大深度、翻转二叉树、对称二叉树、层序遍历、验证 BST、BST 第 K 小、最近公共祖先 |
| 二分 | 搜索插入位置、搜索二维矩阵、查找首尾位置、搜索旋转排序数组 |
| 栈 | 有效的括号、最小栈、每日温度 |

### 剩余 60 题分布

链表 6、二叉树 7、图 4、回溯 8、二分 2、栈 2、堆 3、贪心 4、动态规划 10、多维动态规划 5、技巧 5、矩阵 2、普通数组 1、子串 1。

---

## 二、冲刺期三条纪律

入门期是"每天少量、慢慢吃透"；冲刺期必须换成下面三条，否则时间不够。

1. 只练真高频。剩余 60 题里，P0 的 35 题必须能在 15 分钟内白板手写，P1 能讲清思路并写出核心代码，P2 没有时间可以放弃。
2. 模板优先于题量。同一个模板的题（如回溯五连、DP 三连）放在同一天，学的是模板，不是孤立题目。
3. 旧题重写优先于新题。每天开始前，先用 `dayXX_practice.ipynb` 不看答案重写 2 道旧题，这一步不许跳过。

---

## 三、剩余 60 题优先级

分层依据是国内互联网面经中出现的常见程度，属于经验判断，可按投递岗位调整（投前端岗可以进一步降低 Hard 题权重）。

### P0 必刷（35 题，必须手写熟练）

| 主题 | 题目 |
| --- | --- |
| 链表（4） | 24 两两交换链表中的节点、138 随机链表的复制、148 排序链表、146 LRU 缓存 |
| 二叉树（3） | 543 二叉树的直径、199 二叉树的右视图、105 从前序与中序遍历序列构造二叉树 |
| 图（3） | 200 岛屿数量、994 腐烂的橘子、207 课程表 |
| 回溯（5） | 46 全排列、78 子集、39 组合总和、22 括号生成、79 单词搜索 |
| 二分（1） | 153 寻找旋转排序数组中的最小值 |
| 栈（2） | 394 字符串解码、84 柱状图中最大的矩形 |
| 堆（2） | 215 数组中的第 K 个最大元素、347 前 K 个高频元素 |
| 贪心（1） | 121 买卖股票的最佳时机 |
| 动态规划（5） | 70 爬楼梯、118 杨辉三角、198 打家劫舍、322 零钱兑换、300 最长递增子序列 |
| 多维动态规划（4） | 62 不同路径、64 最小路径和、5 最长回文子串、1143 最长公共子序列 |
| 技巧（5） | 136 只出现一次的数字、169 多数元素、75 颜色分类、31 下一个排列、287 寻找重复数 |

### P1 重点（22 题，尽量都过一遍）

25 K 个一组翻转链表、23 合并 K 个升序链表、295 数据流的中位数、4 寻找两个正序数组的中位数、108 将有序数组转换为二叉搜索树、114 二叉树展开为链表、437 路径总和 III、124 二叉树中的最大路径和、208 实现 Trie（前缀树）、17 电话号码的字母组合、131 分割回文串、51 N 皇后、55 跳跃游戏、45 跳跃游戏 II、763 划分字母区间、279 完全平方数、139 单词拆分、152 乘积最大子数组、416 分割等和子集、72 编辑距离、48 旋转图像、240 搜索二维矩阵 II。

### P2 选刷（3 题，时间够再看）

76 最小覆盖子串、41 缺失的第一个正数、32 最长有效括号。

---

## 四、每日安排（Day 11 - Day 26）

每天 3 道必做 + 1 道加练。加练做不完可以顺延，但必做三题当天必须写完并运行通过。

| Day | 主题 | 必做 | 加练 | 建议文件名 |
| --- | --- | --- | --- | --- |
| 11 | 链表收尾 | 24 两两交换、146 LRU 缓存、138 随机链表的复制 | 148 排序链表 | `day11_linked_list_advanced.ipynb` |
| 12 | 堆与链表困难题 | 215 第 K 大、347 前 K 高频、25 K 个一组翻转 | 23 合并 K 个升序链表 | `day12_heap_and_hard_list.ipynb` |
| 13 | 栈与堆进阶 | 394 字符串解码、84 柱状图最大矩形、295 数据流中位数 | 76 最小覆盖子串 | `day13_stack_and_design.ipynb` |
| 14 | 二叉树（一） | 543 直径、199 右视图、108 有序数组转 BST | 114 展开为链表 | `day14_binary_tree_1.ipynb` |
| 15 | 二叉树（二） | 105 前中序构造、437 路径总和 III、124 最大路径和 | 重写 Day 14 两道 | `day15_binary_tree_2.ipynb` |
| 16 | 图与 Trie | 200 岛屿数量、994 腐烂的橘子、207 课程表 | 208 实现 Trie | `day16_graph_and_trie.ipynb` |
| 17 | 回溯（一） | 46 全排列、78 子集、39 组合总和 | 17 电话号码字母组合 | `day17_backtracking_1.ipynb` |
| 18 | 回溯（二） | 22 括号生成、79 单词搜索、131 分割回文串 | 51 N 皇后 | `day18_backtracking_2.ipynb` |
| 19 | 贪心 | 121 买卖股票、55 跳跃游戏、45 跳跃游戏 II | 763 划分字母区间 | `day19_greedy.ipynb` |
| 20 | 动态规划（一） | 70 爬楼梯、118 杨辉三角、198 打家劫舍 | 279 完全平方数 | `day20_dp_1.ipynb` |
| 21 | 动态规划（二） | 322 零钱兑换、300 最长递增子序列、139 单词拆分 | 152 乘积最大子数组 | `day21_dp_2.ipynb` |
| 22 | 动态规划（三）与网格 | 5 最长回文子串、62 不同路径、64 最小路径和 | 1143 最长公共子序列 | `day22_dp_3.ipynb` |
| 23 | 技巧（一） | 136 只出现一次、169 多数元素、75 颜色分类 | 31 下一个排列 | `day23_tricks_1.ipynb` |
| 24 | 技巧（二）与二分收尾 | 287 寻找重复数、153 旋转数组最小值、48 旋转图像 | 240 搜索二维矩阵 II | `day24_tricks_2.ipynb` |
| 25 | Hard 冲刺 | 72 编辑距离、4 两个正序数组中位数、416 分割等和子集 | 32 最长有效括号 | `day25_hard_sprint.ipynb` |
| 26 | 机动复习日 | 76、41，并重写最不熟的 5 道 P0 | 无 | 不生成新文件 |

每天仍然同步生成 `dayXX_practice.ipynb` 纯享版。

---

## 五、每天怎么练（秋招模式）

### 单题流程（每题上限 45 分钟）

1. 读题 3 分钟，用自己的话说一遍输入和输出。
2. 想思路 10 分钟，只写关键词和复杂度，不写代码。
3. 写代码 20 分钟。
4. 测边界 5 分钟：空输入、只有一个元素、重复元素。
5. 口述 2 分钟：解法专业名 + 时间复杂度 + 空间复杂度 + 为什么这样选。

### 卡壳规则

- 15 分钟没思路：看 notebook 的"思路拆解"，不看参考答案，再想 10 分钟。
- 40 分钟还写不出来：看参考答案，理解后关掉答案，在 `dayXX_practice.ipynb` 里从头重写一遍。
- 任何一道题第一次没独立写出来，第二天必须重写一次。

### 每日收尾（3 行复盘）

- 今天真正学会的模板是什么。
- 卡在哪一步（思路、边界、语法）。
- 明天重写哪两道题。

---

## 六、代码风格：只用通用写法

后续 notebook 的教学代码和参考答案，一律按下面的白名单写，保证面试时能逐行解释。

### 允许并推荐

- 基础语法：`for`、`while`、`if / elif / else`、`def`、`class`
- 容器：`list`、`dict`、`set`、`tuple`、`str`
- 标准库：`collections.deque`（用 `append` 和 `popleft`）、`collections.defaultdict`、`heapq`、`float('inf')`
- 常用内置函数：`len`、`range`、`enumerate`、`sorted`、`min`、`max`、`sum`、`abs`
- 列表推导式只写最简单的一层（如 `[0] * n`、`[x * 2 for x in nums]`）
- 计数统一用普通 `dict` 加 `if x in count` 判断，不默认用 `Counter`

### 禁止使用（面试时不好解释或属于炫技）

- 海象运算符 `:=`
- `functools.lru_cache`、装饰器当默认解法
- `bisect`、`itertools` 直接调用代替手写逻辑
- 位运算技巧（136 只出现一次的数字必须用异或除外）
- 一行流写法、嵌套多层推导式、三元表达式套三元表达式
- 递归不写显式终止条件
- 复杂的 `key=lambda`、`sort` 多级排序技巧
- 满屏类型注解

### 五个统一模板（要背到能默写）

二分查找（左闭右闭）：

```python
left, right = 0, len(nums) - 1
while left <= right:
    mid = (left + right) // 2
    if nums[mid] == target:
        return mid
    elif nums[mid] < target:
        left = mid + 1
    else:
        right = mid - 1
return -1
```

回溯：

```python
def backtrack(路径, 选择列表):
    if 满足结束条件:
        收集结果
        return
    for 选择 in 选择列表:
        做选择
        backtrack(新的路径, 新的选择列表)
        撤销选择
```

网格搜索：

```python
from collections import deque

directions = [(1, 0), (-1, 0), (0, 1), (0, -1)]
queue = deque()
queue.append((起点行, 起点列))
visited = set()
visited.add((起点行, 起点列))
while queue:
    row, col = queue.popleft()
    for drow, dcol in directions:
        new_row = row + drow
        new_col = col + dcol
        # 判断越界、是否访问过、是否可走，再入队
```

动态规划：

```python
# dp[i] 表示：……（先写清楚这一行，再写代码）
dp = [0] * (n + 1)
dp[0] = 初始值
for i in range(1, n + 1):
    dp[i] = 由前面的状态推出
return dp[n]
```

小顶堆求前 K 大：

```python
import heapq

heap = []
for num in nums:
    heapq.heappush(heap, num)
    if len(heap) > k:
        heapq.heappop(heap)
return heap[0]
```

链表建表继续沿用已经学过的 `dummy` + `tail` + `for` 逐个创建节点的方法，不换写法。

---

## 七、时间不够时的裁剪方案

### 只剩 10 天

只做 Day 11 - Day 20 的必做三题（30 题），加练全部放弃，Day 21 - Day 26 不执行。

### 只剩 7 天（21 题）

146、215、200、46、22、70、322、300、121、136、169、75、5、62、543、199、105、394、84、24、207。

### 只剩 3 天（12 题）

146、215、200、46、22、70、322、121、136、5、543、394。

### 如果投的是前端岗

可以进一步放弃 84、4、72、51、32 这几道 Hard，把时间还给 JavaScript、CSS、框架原理和项目复盘。

---

## 八、给后续助手的执行说明

1. 生成 Day 11 及之后的 notebook 前，先读 `PROJECT_CONTEXT.md`、`NOTEBOOK_SPEC.md`、本文件，以及 `day10_stack_and_rain_water.ipynb`。
2. 严格按第四节的题单和主题顺序生成，不自行调换题目顺序。
3. 代码严格遵守第六节的白名单和五个统一模板，不引入新的炫技写法。
4. 每道题仍然要"做题前先补"，但只补这道题真正需要的新知识；DP 题重点补"状态定义"和"转移方程"的推导过程，不要直接给公式。
5. 每天仍要生成 `dayXX_practice.ipynb`，链表题继续遵守单格自包含规范。
6. 新生成的文件要做 notebook JSON 格式校验。
