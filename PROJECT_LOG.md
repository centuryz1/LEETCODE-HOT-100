# 项目日志

## 2026-07-29

### 本次目标

建立 LeetCode Hot 100 学习项目的跨设备连续性规范，避免换设备后生成的学习文件风格不一致。

### 已完成

- 创建 `PROJECT_CONTEXT.md`，记录项目目标、当前产物、持久规范和交接说明。
- 创建 `NOTEBOOK_SPEC.md`，固化每日 notebook 的生成结构、题目模块结构、练习代码块规范和校验方式。
- 创建 `NEXT_SESSION_PROMPT.md`，提供换设备时可直接复制给助手的提示词。
- 创建 `TODO.md`，记录后续学习任务。
- 初始化本地 Git 仓库，并将 `origin` 关联到 `https://github.com/centuryz1/LEETCODE-HOT-100.git`。

### 当前状态

项目已经不依赖单个助手的聊天记忆。后续任何设备都可以通过读取项目规范文件继续生成同风格的学习 notebook。本地 Git 仓库已关联 GitHub 远程仓库。

### 验证

本次新增的是 Markdown 项目管理文件，不需要 notebook JSON 校验。已通过 `git remote -v` 检查远程仓库关联。

### 下一步

继续 Day 01 学习，完成或复盘前 2-4 道题；之后按 `NOTEBOOK_SPEC.md` 生成 Day 02。

## 2026-07-29

### 本次目标

根据用户建议，为每日题解补充专业解法名称，方便秋招面试表达。

### 已完成

- 在 `day01_hash_and_two_pointers.ipynb` 的 4 道题中补充解法名称。
- 在用户动手写的代码块注释中加入 `# 解法：...`。
- 更新 `NOTEBOOK_SPEC.md`，要求后续 Day notebook 都包含中文专业名和常见英文说法。

### 验证

- 已计划校验 `day01_hash_and_two_pointers.ipynb` 的 JSON 格式。

### 下一步

继续按新规范生成后续学习文件。

## 2026-07-29

### 本次目标

补充刷题常用 Python 字典知识，服务 Day 01 的哈希表学习。

### 已完成

- 在 `前置知识补充.ipynb` 中新增 `B03-1. dict 常用操作`。
- 在 `前置知识补充.ipynb` 中新增 `B03-2. defaultdict 常用操作`。
- 补充了创建、查询、`get`、`in`、新增、修改、删除、遍历、计数、分组和常见误区。

### 验证

- 已校验 `前置知识补充.ipynb` 为合法 ipynb JSON。

### 下一步

Day 01 学习两数之和、字母异位词分组时，可以先查阅这两个字典小节。

## 2026-07-29

### 本次目标

为 Day 01 创建无提示练习题纯享版，并将该产物固化为后续每日学习文件的配套要求。

### 已完成

- 创建 `day01_hash_and_two_pointers_practice.ipynb`，随后按用户偏好重命名为 `day01_practice.ipynb`。
- 纯享版只包含题目描述和空函数代码块，不包含提示、思路、测试、参考答案和面试表达。
- 更新 `PROJECT_CONTEXT.md`，记录练习题纯享版命名约定和验收要求。
- 更新 `NOTEBOOK_SPEC.md`，新增练习题纯享版 notebook 规范。
- 更新 `TODO.md`，记录后续每日都要生成配套练习题纯享版。

### 验证

- 已计划校验 `day01_hash_and_two_pointers_practice.ipynb` 的 JSON 格式。

### 下一步

后续生成 Day 02 时，同时生成 `day02_practice.ipynb`。

## 2026-07-29

### 本次目标

解释用户不理解的“双指针”概念，并补充到模块化前置知识库。

### 已完成

- 在 `前置知识补充.ipynb` 的模块 D 中新增 `D01. 双指针`。
- 补充双指针的概念、作用、左右指针、快慢指针、同向双指针、识别信号和常见误区。
- 加入最小 Python 示例，方便用户逐格运行观察。

### 验证

- 已校验 `前置知识补充.ipynb` 为合法 ipynb JSON。

### 下一步

Day 01 的“移动零”和“盛最多水的容器”可以结合 D01 双指针模块一起学习。
## 2026-07-31

### 本次目标

恢复并完善 Day 02 的“无重复字符的最长子串”学习内容。

### 已完成

- 补全题目的输入、输出、关键限制和三个示例。
- 补充暴力思路、滑动窗口优化过程、复杂度和逐步跟踪代码。
- 完善用户动手代码块、测试用例和面试表达。
- 同步补全 `day02_practice.ipynb` 中对应题目的完整题面和代码注释。

### 验证

- 已校验两份 Day 02 notebook 的 JSON 格式及第 3 题关键内容。

## 2026-07-31

### 本次目标

创建 Day 03 前缀和与连续数组进阶学习材料，并延续每日详细版与纯练习版规范。

### 已完成

- 创建 `day03_prefix_sum_and_array.ipynb`，包含 4 道 Hot 100 题的完整教学模块。
- 创建 `day03_practice.ipynb`，只保留完整题面和空白练习代码。
- 在 `前置知识补充.ipynb` 中补充 `deque`、滑动窗口、前缀和与前缀积、动态规划和单调队列。
- 更新项目上下文和后续学习任务。

### 验证

- 已校验全部 notebook 为合法 JSON。
- Day 03 四个参考答案及边界用例运行通过。
- `day03_practice.ipynb` 已确认不包含参考答案、过程跟踪和测试代码。

### 下一步

先完成 Day 03 前两道必做题；状态好再完成第 3 题和单调队列加练。

## 2026-08-03

### 本次目标

针对用户在“最长连续序列”中混淆 `if` 与 `while` 的问题，创建 if 与 while 选择专题。

### 已完成

- 创建 `专题_if和while怎么选.ipynb`。
- 对比用户的 `if` 版本和正确的 `while` 版本，并加入逐轮运行跟踪。
- 补充三问判断法、`for`/`if`/`while` 分工、死循环误区和 4 组专项练习。
- 增加参考答案与自动断言测试。

### 验证

- notebook 已通过 nbformat 4.5 JSON 解析校验。
- 全部代码单元和 9 个参考答案断言运行通过。

### 下一步

完成专题中的 4 组练习，然后不看答案重写“最长连续序列”。

## 2026-08-03

### 本次目标

创建 Day 04 数组与矩阵学习材料，暂时跳过 Hot 100 困难题。

### 已完成

- 创建 `day04_array_and_matrix.ipynb`，包含 4 道中等难度 Hot 100 题。
- 创建 `day04_practice.ipynb`，只保留完整题面和空白练习代码。
- 在 `前置知识补充.ipynb` 中新增二维列表与矩阵、排序后线性扫描、模拟与边界模块。
- 将 if/while 专题知识应用到螺旋矩阵的循环与边界判断讲解中。
- 更新项目上下文和后续任务。

### 验证

- 两份 Day 04 notebook 均通过 nbformat 4.5 JSON 解析。
- 详细版全部代码单元、四个参考答案及额外边界用例运行通过。
- `day04_practice.ipynb` 已确认不包含提示、测试、过程跟踪或参考答案。

### 下一步

先完成合并区间和轮转数组；状态好再进入两道矩阵题。
## 2026-08-03

### 本次目标

创建 ACM 与 LeetCode 输入输出专题，帮助用户适应秋招笔试的标准输入输出模式。

### 已完成

- 创建 `专题_ACM和LeetCode输入输出.ipynb`。
- 对比 LeetCode 函数形式和 ACM 完整程序形式。
- 补充整数、数组、字符串、矩阵、多组测试和 EOF 的读取方法。
- 补充单值、列表、矩阵的输出格式和 `solve()` 模板。
- 提供 4 组专项练习、正式提交版示例和自动断言测试。

### 验证

- notebook 已通过 nbformat 4.5 JSON 解析校验。
- 14 个代码单元和参考答案的额外测试全部运行通过。

### 下一步

完成专题练习，并把已经掌握的一道 LeetCode 题改写成 ACM 完整程序。
## 2026-08-03

### 本次目标

在模块化前置知识中补充 if、elif、else 与 continue 的区别和用法。

### 已完成

- 在 `前置知识补充.ipynb` 中新增 `B06. if、elif 和 else`。
- 通过对照代码说明多个独立 `if` 可能同时执行，而分支链只执行第一个命中的分支。
- 补充 `elif` 条件顺序、互斥分支和常见误区。
- 新增 `B07. continue 的用法`，对比 `continue`、`break` 和 `pass`。
- 补充 `while + continue` 的死循环风险和安全写法。

### 验证

- notebook 已通过 nbformat 4.5 JSON 解析校验。
- 从新增章节开始的 18 个代码单元和断言测试全部运行通过。

### 下一步

完成两节末尾的小练习，并尝试在自己的循环代码中口述每个分支的作用。

## 2026-08-03

### 本次目标

总结三数之和中为什么使用 `while left < right`，帮助用户理解双指针的循环条件。

### 已完成

- 创建 `专题_双指针为什么用while_left小于right.ipynb`。
- 从搜索区间和指针职责出发，推导 `while left < right`，并对比 `if` 与 `while`。
- 解释为什么使用 `<` 而不是 `<=`，以及排序后如何根据和移动左右指针。
- 总结三数之和的三处去重，并指出 `target` 参数与固定比较 `0` 的常见错误。
- 提供标准模板、逐轮跟踪、专项练习和参考答案。

### 验证

- notebook 已通过 nbformat 4.5 JSON 解析校验，共 26 个单元。
- 6 个代码单元全部运行通过，三数之和两个版本、两数之和练习及额外边界用例测试通过。

### 下一步

完成专题练习，然后不看答案独立写出三数之和，并口述循环不变量。

## 2026-08-04

### 本次目标

为零基础用户系统讲解 Docker，并通过 Python Web 服务完成一次容器化实践。

### 已完成

- 创建 `专题_Docker从入门到实践.ipynb`。
- 讲解 Dockerfile、镜像、容器、仓库和构建上下文的关系。
- 对比容器与虚拟机，讲解端口、环境变量、挂载、数据卷、网络和 Compose。
- 提供基于 Python 标准库的 Web 服务、Dockerfile、构建与运行完整示例。
- 补充常用命令、排错顺序、基础最佳实践、练习和面试表达。

### 验证

- notebook 已通过 nbformat 4.5 JSON 解析校验，共 35 个单元。
- 9 个 Python 代码单元全部运行通过，端口映射、示例文件和命令生成的额外测试通过。
- 当前电脑未检测到 `docker` 命令，因此未进行真实镜像构建和容器启动。

### 下一步

安装并启动 Docker Desktop，依次完成 hello-world、nginx 和 Python Web 服务实践。

## 2026-08-04（Docker 教程简化）

### 调整原因

第一版一次引入了端口、挂载、网络和 Compose 等大量概念，阅读负担过重，不适合当前零基础阶段。

### 已调整

- 将教程改为约 20 分钟的小白第一课。
- 当前只要求掌握 Dockerfile、镜像、容器，以及 `docker build`、`docker run`。
- 将示例简化为打印问候语的 Python 命令行程序。
- 把端口映射改成可选彩蛋，把 Compose、数据卷和网络移到后续学习清单。
- 增加短练习和明确的“今天学到哪里就够了”。

### 验证

- notebook 已通过 nbformat 4.5 结构校验，共 24 个短单元。
- 6 个 Python 代码单元全部运行通过，未安装 Docker 时也不会报错。

## 2026-08-04（Docker 教程难度再平衡）

### 调整原因

极简版缺少足够的实践深度，无法满足“系统学习并结合例子”的目标。

### 已调整

- 改为“必学、实践、选学”三级结构，允许分两次学习。
- 保留零基础解释，同时恢复 Python 容器化和 Web 服务完整示例。
- 加入端口映射、环境变量、容器生命周期和实用排错流程。
- 数据卷和 Compose 只讲用途，不展开底层细节。

### 验证

- notebook 已通过 nbformat 4.5 结构校验，共 30 个分层单元。
- 8 个 Python 代码单元全部运行通过，命令生成和端口映射额外测试通过。

## 2026-08-04（时间与空间复杂度专题）

### 本次目标

帮助零基础用户系统理解时间复杂度和空间复杂度，并掌握秋招面试中的计算与表达方法。

### 已完成

- 创建 `专题_时间复杂度和空间复杂度.ipynb`。
- 从输入规模和增长趋势解释大 O 表示法及常见复杂度。
- 总结顺序代码、嵌套循环、条件分支、减半循环和多个输入的计算规则。
- 讲解额外空间、辅助容器、原地算法、递归调用栈和输出空间口径。
- 加入 Python 常用操作、两数之和、三数之和、二分查找和滑动窗口的面试分析。
- 提供秋招回答模板、常见误区和专项练习。

### 验证

- notebook 已通过 nbformat 4.5 结构校验，共 38 个单元。
- 10 个 Python 代码单元全部运行通过，线性、平方、对数、递归、哈希表和二分查找的额外断言测试通过。

### 下一步

完成专题练习，并尝试不看答案口述三道典型算法的时间与空间复杂度。

## 2026-08-04（Python 基础知识手册）

### 本次目标

创建一份适合零基础用户长期查阅的 Python 基础手册，帮助巩固刷题和秋招笔试所需语法。

### 已完成

- 创建 `Python基础知识手册_算法与秋招版.ipynb`。
- 覆盖变量、数据类型、输入输出、运算符、字符串和四种核心容器。
- 系统讲解条件、循环、函数、参数、返回值和作用域。
- 补充切片、推导式、排序、内置函数、无穷值和常见 Python 写法。
- 讲解 Counter、defaultdict、deque、heapq 及常用算法数据结构。
- 补充可变对象、拷贝、二维列表、LeetCode/ACM 形式、调试和断言测试。
- 提供常用算法模板、五道综合练习和七天巩固计划。

### 验证

- notebook 已通过 nbformat 4.5 结构校验，共 79 个短单元。
- 33 个 Python 代码单元全部运行通过，基础函数、容器操作和五道综合练习的额外测试通过。

### 下一步

按七天计划分模块学习，不追求一次看完；每学完一天，独立重写该模块的核心示例。

## 2026-08-04（零碎知识库重命名与 Counter）

### 本次目标

将前置知识文件改为可持续记录细节的零碎知识库，并补充 Counter 计数器。

### 已完成

- 将 `前置知识补充.ipynb` 重命名为 `零碎知识补充.ipynb`。
- 更新项目上下文、生成规范、跨设备提示、TODO 和 Day notebook 中的当前引用。
- 新增 `B03-3. Counter 计数器`，承接 dict 与 defaultdict 章节。
- 讲解 Counter 创建、读取、most_common、update、subtract 和工具选择。
- 补充字母异位词示例、常见误区、专项练习和参考答案。

### 验证

- `零碎知识补充.ipynb` 已通过 nbformat 4.5 结构校验，共 114 个单元。
- 全部 45 个代码单元运行通过，Counter 章节和字母异位词额外测试通过。
- 除历史日志外，项目当前文件中已不存在旧文件名引用。

### 下一步

以后遇到不会、易忘或容易混淆的通用细节，优先按模块补充到 `零碎知识补充.ipynb`。

## 2026-08-04（Pandas 数据分析专题）

### 本次目标

创建一份难度适中的 Pandas 专题，帮助用户完成从表格读取、清洗到统计分析的完整流程。

### 已完成

- 创建 `专题_Pandas从入门到数据分析.ipynb`。
- 使用订单数据贯穿 Series、DataFrame、索引、选择、筛选和修改。
- 补充缺失值、重复值、类型转换、字符串与日期处理。
- 讲解 value_counts、groupby、agg、transform 和 pivot_table。
- 讲解 merge、concat、键关系校验以及 CSV、Excel 读写。
- 补充向量化、方法链、Copy-on-Write、常见错误和面试表达。
- 提供综合业务案例、专项练习、参考答案和七天学习计划。

### 验证

- notebook 已通过 nbformat 4.5 结构校验，共 64 个单元。
- 本机 pandas 版本为 2.3.3，27 个代码单元全部运行通过。
- 筛选、销售额、分组、透视表、多表连接和综合练习的额外断言测试通过。

### 下一步

完成订单数据练习，并用一份自己的 CSV 或 Excel 重做读取、清洗、分组和导出流程。

## 2026-08-04（Day 03 和为 K 的子数组暴力解法）

### 本次目标

为 Day 03 的“和为 K 的子数组”补充完整暴力解法，建立从枚举到前缀和优化的学习递进。

### 已完成

- 将专业解法补充为暴力枚举与前缀和 + 哈希表两种。
- 讲解枚举起点、向右扩展终点并维护累计和的推导过程。
- 对比重复切片求和的 `O(n³)` 与累计求和的 `O(n²)`。
- 新增过程跟踪、暴力参考函数、复杂度分析、面试表达和边界测试。
- 重写前缀和优化动机，使两种解法自然衔接。

### 验证

- Day 03 notebook 已通过 nbformat 4.5 结构校验，共 52 个单元。
- 全部 18 个代码单元运行通过。
- 暴力解法与前缀和解法完成 200 组随机小数组交叉验证，结果一致。

### 下一步

先独立写出 `O(n²)` 暴力解法，再根据重复计算问题推导 `O(n)` 前缀和 + 哈希表解法。

## 2026-08-04（Python 字典专题）

### 本次目标

创建一份面向零基础和刷题场景的 Python 字典专题，帮助用户形成稳定的字典使用习惯。

### 已完成

- 创建 `专题_Python字典从入门到刷题.ipynb`。
- 讲解键值映射、可哈希键、创建、查询、新增、修改、删除和遍历。
- 重点对比 `[]`、`get()`、`in`、`items()`、`setdefault()` 和 `update()`。
- 将刷题用途归纳为计数器、存在性记录、下标映射和分组容器四种角色。
- 补充键设计、多个下标、推导式、嵌套字典、拷贝和复杂度。
- 使用两数之和、字母异位词分组、和为 K 的子数组串联典型用法。
- 提供高频误区、调试方法、专项练习、参考答案和五天计划。

### 验证

- notebook 已通过 nbformat 4.5 结构校验，共 69 个单元。
- 29 个 Python 代码单元全部运行通过。
- 计数、重复判断、下标分组、合并计数、两数之和、前缀和与异位词分组的额外测试通过。

### 下一步

每次写字典前先口述“键是什么、值是什么”，再完成六道专项练习。

## 2026-08-05（列表重复 `[value] * n`）

### 本次目标

在零碎知识库中补充 `[1] * n` 的含义、用途和二维列表陷阱。

### 已完成

- 新增 `B02-1. 列表重复：[value] * n`。
- 讲解序列重复、常见初始化方式以及零次和负数次重复。
- 说明一维不可变值初始化通常安全。
- 对比错误的 `[[0] * m] * n` 与正确的列表推导式写法。

### 验证

- `零碎知识补充.ipynb` 已通过 nbformat 4.5 结构校验，共 120 个单元。
- 全部 48 个代码单元运行通过，列表重复与二维列表引用测试通过。

### 下一步

看到 `[value] * n` 时先翻译为“把这个列表重复 n 次”，二维列表则优先使用推导式。

## 2026-08-05（range 倒序遍历）

### 本次目标

解释 `range(n - 1, -1, -1)` 的含义，并补入零碎知识库。

### 已完成

- 新增 `B04-1. range 倒序遍历`。
- 按起点、终点和步长拆解三个参数。
- 解释终点为什么写 `-1`，以及 range 不包含终点的规则。
- 对比下标倒序、`reversed(nums)` 和 `nums[::-1]`。
- 补充后缀计算、倒序动态规划和空列表边界。

### 验证

- `零碎知识补充.ipynb` 已通过 nbformat 4.5 结构校验，共 126 个单元。
- 全部 50 个代码单元运行通过，正序边界、倒序边界和空范围测试通过。

### 下一步

看到三个参数的 range 时，依次口述“从哪里开始、在哪里之前停止、每次走几步”。

## 2026-08-10（deque 命令补充）

### 本次目标

扩充零碎知识库中的 deque 命令、使用场景和常见误区。

### 已完成

- 扩展 `C01. 队列与双端队列 deque`。
- 补充 append、appendleft、pop、popleft 与两端查看。
- 补充 extend、extendleft、rotate、maxlen、clear、remove、count 和 reverse。
- 对比 deque 实现队列、栈以及列表的适用场景。
- 新增复杂度速查、常见误区、专项练习和参考答案。

### 验证

- 待完成零碎知识 notebook 结构和全部代码单元校验。

### 下一步

优先熟练前四个核心命令，并能根据 FIFO 或 LIFO 选择正确的弹出方向。

## 2026-08-10（题目前置知识就近讲解规则）

### 调整原因

完整 deque 命令章节放在零碎知识库中，与实际使用它的“滑动窗口最大值”距离过远，不利于学完立即应用。

### 已调整

- 撤回零碎知识库中新扩展的 23 个 deque 详细单元，只保留简短通用说明。
- 在 Day 03 第 4 题标题前新增 deque 必要知识。
- 只讲本题使用的 append、pop、popleft、队首队尾访问和非空判断。
- 结合窗口过期下标和单调队列解释每个命令的职责。
- 更新项目规范、上下文和跨设备提示，要求以后把新知识放在相关题目前。

### 验证

- Day 03 已通过 nbformat 4.5 结构校验，共 60 个单元、21 个代码单元。
- 滑动窗口最大值参考答案通过 100 组随机用例测试。
- `零碎知识补充.ipynb` 共 126 个单元、50 个代码单元，全部运行通过。
- 已确认 deque 前置知识位于 LeetCode 239 题面之前。

### 下一步

以后生成题目时，先判断本题新增了哪些知识，再在题目标题前提供最小可运行示例。

## 2026-08-10（Day 04 前置知识审查与补充）

### 审查结果

- 原文件只有统一的“今日前置知识速览”，四道题前缺少就近的可运行讲解。
- 按“题目前置知识就近讲解规则”完成调整。

### 已补充

- 合并区间前：区间下标、`sort(key=...)`、`lambda`、`merged[-1]` 和重叠判断。
- 轮转数组前：`k %= n`、原地修改、左右指针反转。
- 螺旋矩阵前：二维列表访问、行列数量和四条边界。
- 矩阵置零前：集合的 `add` 与 `in`、行列标记和两遍扫描。
- 每块前置知识都配有最小可运行代码，并紧挨对应题目标题。

### 验证

- `day04_array_and_matrix.ipynb` 共 56 个单元、20 个代码单元，全部顺序运行通过。
- 四道题的前置知识位置检查通过。
- 参考答案通过区间相接、超长轮转步数、矩形与单行单列矩阵等额外测试。

### 下一步

可以从 Day 04 开始学习；每道题先运行“做题前先补”，再阅读题目并独立作答。

## 2026-08-10（生成 Day 05 至 Day 09）

### 本次目标

按照秋招面试高频优先的顺序，一次准备接下来五天的详细学习材料和无提示练习题。

### 已完成

- Day 05：链表基础与双指针，共 4 题。
- Day 06：链表进阶与虚拟头节点，共 4 题。
- Day 07：二叉树基础与递归，共 4 题。
- Day 08：层序遍历、二叉搜索树与公共祖先，共 4 题。
- Day 09：二分查找高频模板，共 4 题。
- 每天安排 3 道必做题和 1 道加练题，不包含困难题。
- 为五天分别生成对应的 `dayXX_practice.ipynb` 纯享版。
- 所有新知识均在相关题目前设置“做题前先补”和最小可运行示例。

### 验证

- 5 份学习 notebook 均为 50 个单元、20 个代码单元，全部顺序运行通过。
- 5 份练习 notebook 均为 10 个单元、4 个空函数，不含提示、过程、测试或参考答案。
- 20 个参考答案通过典型与边界用例。
- Day 09 的二分答案额外通过 925 组随机和矩形矩阵测试。

### 下一步

用户先从 Day 04 开始学习；后续按 Day 05 至 Day 09 顺序推进，每天开始前用练习版重写前一天至少两道必做题。

## 2026-08-10（生成 Day 10 栈与接雨水）

### 已完成

- 创建 `day10_stack_and_rain_water.ipynb`。
- 创建无提示练习版 `day10_practice.ipynb`。
- 题单为有效的括号、最小栈、每日温度、接雨水。
- 每题前就近补充列表栈、类与 `self`、单调栈下标、单列接水公式。
- 接雨水按按列枚举、前后缀最大值、双指针水位法、单调栈分层结算四层讲解。

### 验证

- 学习版共 54 个单元、22 个代码单元，全部顺序运行通过。
- 练习版共 10 个单元、4 个空练习代码单元。
- 四道题的前置知识位置与 notebook 4.5 格式检查通过。
- 每日温度通过 400 组随机对照测试。
- 接雨水的双指针、前后缀和单调栈三种解法共同通过 800 组随机对照测试。

### 下一步

按既定顺序学习到 Day 10；接雨水第一次至少掌握前后缀解法并能解释双指针，不强求立即默写单调栈版本。

## 2026-08-11（补充轮转数组切片知识）

### 已补充

- 在 Day 04 轮转数组题目标题前扩充切片前置知识。
- 讲清 `nums[start:stop]` 的左闭右开规则和负数下标。
- 逐步解释 `nums[-k:]`、`nums[:-k]` 分别取得哪一段，以及为什么拼接后是向右轮转。
- 对比 `nums = ...` 与 `nums[:] = ...`，说明 LeetCode 原地修改要求。
- 补充 `k > n`、`k = 0` 和切片额外空间复杂度。
- 增加可运行代码观察两段切片、列表对象身份与部分反转。

### 验证

- Day 04 共 56 个单元、20 个代码单元，全部顺序运行通过。
- 切片知识位于 LeetCode 189 题目标题之前。
- 切片轮转与三次反转通过 500 组随机对照测试，包括取余后 `k = 0` 的情况。

## 2026-08-11（创建下标、range 与切片专题）

### 创建原因

轮转数组题目前同时出现 `n-1`、`k-1`、`range`、正负下标和切片，集中讲解信息过多，因此拆成独立基础专题。

### 已完成

- 创建 `专题_下标_range与切片.ipynb`。
- 从“元素数量”和“下标编号”的区别开始解释 `n-1`。
- 对比“第 k 个元素”与“下标 k 的元素”，解释 `k-1`。
- 系统讲解 `range(n)`、`range(1,n)`、`range(1,n+1)` 和倒序 range。
- 讲解切片左闭右开、四种常用切片、负下标、步长和列表拷贝。
- 对比左闭右开与左右都包含的区间，解释 `right+1`。
- 用轮转数组串联 `n-k`、`k-1`、`n-1`、切片写回和三次反转。
- 增加分模块练习、参考答案、高频写法速查表和最终自检。

### 验证

- 专题共 63 个单元、29 个代码单元，全部顺序运行通过。
- notebook 4.5 格式和必备知识点检查通过。
- 切片轮转与三次反转通过 500 组随机对照测试。

### 下一步

先学习专题 A、B、C 模块，能够自己画出下标后，再回到 Day 04 轮转数组；D 到 H 模块可以边做题边复习。

## 2026-08-13（创建数据结构体系专题）

### 本次目标

为 LeetCode Hot 100 和秋招面试建立系统化数据结构知识框架，避免只会按题背模板。

### 已完成

- 创建 `专题_数据结构体系与Hot100常见用法.ipynb`。
- 区分数据结构与双指针、滑动窗口、DFS/BFS、动态规划等算法方法。
- 系统讲解数组与字符串、哈希表、栈、队列、链表、树与 BST、堆、图与网格、Trie、并查集。
- 每类结构包含核心模型、Python 表示、常用操作、复杂度、Hot 100 识别信号、高频模式和易错点。
- 补充前缀和加哈希表、LRU、单调结构、拓扑排序等复合结构。
- 增加 Hot 100 选型决策树、关键词对照表、复杂度总表和结构识别练习。
- 给出七天体系化学习路线和三道综合代码练习。

### 验证

- 专题共 97 个单元、28 个代码单元，全部顺序运行通过。
- notebook 4.5 格式、九类核心结构、选型图和复杂度表检查通过。
- 哈希、堆、拓扑排序、Trie、并查集与综合练习答案验证通过。
- 图连通性答案通过 300 组随机对照测试。

### 下一步

先学习总地图和数组、哈希表、栈队列三章，再结合已经完成的 Day 01 到 Day 03 与 Day 10 复习；链表和树章节对应 Day 05 到 Day 08。

## 2026-08-13（链表题代码格独立化）

### 调整原因

链表节点定义、建表、目标函数和测试分散在多个代码格时，阅读和运行顺序不清晰，并且容易出现跨格依赖。

### 已调整

- 重组 Day 05 与 Day 06 的 8 道链表题。
- 每道“你来写”代码格都包含 `ListNode`、建表工具、目标函数、样例建表和测试。
- 每道参考答案代码格重新从节点定义开始，可以在全新 Python 环境独立运行。
- Day 05 与 Day 06 练习纯享版同步改为每题一个自包含代码格，目标函数保留 `pass`。
- 为相交链表增加真正共享节点对象的建表函数。
- 为环形链表增加按 `pos` 创建环和返回入口节点的建表函数。
- 删除原本分散的旧测试代码格，同时保留题面、前置知识、过程解释和面试表达。
- 将链表题单格自包含规则写入 `NOTEBOOK_SPEC.md` 和项目上下文。

### 验证

- 8 个学习练习代码格、8 个完整参考答案格均可在全新命名空间中单独运行。
- 8 个练习纯享版代码格均可独立运行且不包含参考答案。
- Day 05 与 Day 06 整本顺序运行通过。
- 8 道链表题共通过 800 组随机测试。

## 2026-08-13（创建 class、对象与链表建表专题）

### 本次目标

解决学习链表时看不懂 `class ListNode`、`self.val`、`self.next` 和建表代码的问题。

### 已完成

- 创建 `专题_class对象self与链表建表.ipynb`。
- 从普通变量过渡到类和对象，解释类是类型说明、对象是实际创建的数据。
- 逐行讲解 `__init__`、`self`、属性和方法，并澄清 `self.val = val` 两侧含义。
- 用对象引用解释 `next` 为什么能指向另一个节点，以及变量移动和修改链表指向的区别。
- 从单节点、两个节点到 `1 -> 2 -> 3`，完整演示手动建表和遍历。
- 拆解 `dummy`、`tail`、`tail.next = ListNode(value)` 和 `return dummy.next`。
- 增加分级练习、参考答案、常见报错和学习验收标准。

### 验证

- 专题共 79 个单元、36 个代码单元。
- 所有代码格顺序运行通过，并且每个代码格均可在全新命名空间单独运行。
- `build_list` 的普通输入与空列表结果验证通过。

### 下一步

先学习 A 到 D 章，再学习 G 到 J 章并完成建表练习；能够不看答案写出 `ListNode` 和 `build_list` 后，再回到 Day 05 链表题。

## 2026-08-20（创建 Python 文件调用与算法项目专题）

### 本次目标

从零解释 `if __name__ == '__main__'`，并建立从单个 Python 文件、笔试输入输出到规范算法工程项目的完整认识。

### 已完成

- 创建 `专题_if_name_main与Python文件调用及算法项目.ipynb`。
- 逐字拆解 `__name__`、`__main__`、顶层代码、导入副作用和 `main()`。
- 覆盖脚本、模块、包、`python -m`、`__main__.py`、`python -c`、Notebook、控制台入口和子进程等主要调用方式。
- 讲解模块搜索路径、绝对与相对导入、`__init__.py`、模块缓存和常见导入错误。
- 系统整理函数参数、标准输入、命令行参数、环境变量、文件输入、标准输出、错误输出和日志。
- 区分核心代码模式、ACM 完整程序模式、在线编辑器、共享 IDE 和白板式面试，并提供高频输入输出模板。
- 给出刷题仓库与算法工程项目目录，说明 `src/`、`tests/`、`notebooks/`、`configs/`、`pyproject.toml` 等职责。
- 增加多文件项目实测、专项练习、参考答案、面试速查表和项目自检清单。

### 验证

- 专题共 94 个单元、21 个代码单元，notebook 4.5 格式检查通过。
- 全部代码格顺序执行通过，每个代码格也可在全新命名空间独立运行。
- 直接运行与导入差异、命令行调用、标准输入解析和多文件项目均完成实际执行验证。

### 下一步

先学习 A、B、E、F 四章，再完成 I02 和 I03；能够把一道 LeetCode 函数独立改写成 ACM 完整程序后，再学习 G、H 两章的工程结构。

## 2026-08-24（创建 Git 常用命令速查）

### 已完成

- 创建 `专题_Git常用命令速查.ipynb`。
- 用工作区、暂存区、本地仓库和远程仓库解释 Git 基本流程。
- 整理日常提交推送、查看差异、分支、撤销、stash、冲突、远程仓库和 `.gitignore` 命令。
- 增加当前学习仓库推荐流程、高频报错和一页速查表。
- 对 `reset --hard`、`clean -fd` 和强制推送进行风险提示。

### 验证

- notebook 为合法的 nbformat 4.5 文件，共 23 个 Markdown 单元。
- 必备 Git 命令覆盖检查通过。
- 文件不包含代码单元，不需要启动 Jupyter 内核即可阅读。

## 2026-08-26（固化已学写法优先复用规则）

### 调整原因

同一类基础操作如果在后续题目中突然更换函数名称、参数或实现方式，会让初学者重复理解工具代码，干扰对题目算法本身的学习。

### 已完成

- 在 `NOTEBOOK_SPEC.md` 增加“前面学过的写法优先复用”规则。
- 普通链表建表优先沿用 `dummy + tail + for` 逐个创建节点的通用方法。
- 环形、相交等特殊结构只能在普通方法上做最小扩展；确需新辅助函数时，必须先解释旧方法哪里不够。
- 要求同一 Day、相邻 Day、练习版和参考答案保持同类工具函数一致。
- 明确教学代码采用社区常见、直观、可逐行口述的标准写法，避免炫技和不必要封装。
- 同步更新 `PROJECT_CONTEXT.md` 与 `NEXT_SESSION_PROMPT.md`，保证新设备或新 Agent 继续遵守。
- 未修改用户已经调整的 Day 05 链表代码。

### 后续校验

每次生成新 Day notebook 时，先读取最近相关题目的工具函数；完成后对函数名、参数、建表循环和特殊结构扩展方式做一致性检查。

## 2026-08-26（修复 Day 05 后续一致性问题）

### 修复范围

- 修复 Day 05 学习版中相交链表和反转链表练习格的语法错误。
- 将 Day 05 练习版的三份相交链表代码整理为“自包含练习 + 一份 ACM 答案”，删除重复实现。
- Day 05 与 Day 06 统一使用 `ListNode`、`build_list` 和 `list_values`。
- 26 份普通建表工具统一为 `dummy + tail + for` 逐个创建节点。
- 6 份环形链表工具统一为普通建表的最小扩展：额外记录 `cycle_entry`，最后连接尾节点。
- Day 05 与 Day 06 链表代码不再使用节点列表推导式和三元表达式。
- Day 10 最小栈接口由 `get_min()` 改为 LeetCode 要求的 `getMin()`，参考答案类可直接使用 `MinStack`。

### 验证

- Day 05 到 Day 10 共 12 份 notebook 均通过 nbformat JSON 与 Python 语法检查。
- Day 05、Day 06 的所有链表代码格均可在全新命名空间独立执行。
- `build_list`、`list_values`、`build_cycle_list` 的 AST 一致性检查通过。
- Day 05、Day 06 链表参考答案与 Day 10 最小栈完成回归测试。
- 无效环入口下标会明确抛出 `ValueError`，不再静默创建无环链表。

## 2026-09-23（制定 Day 11 之后秋招冲刺计划）

### 调整原因

秋招笔面试临近，入门期"每天少量、慢慢吃透"的节奏已经不适合，需要把剩余题目按面试出现频率重排，优先保证高频题能独立手写。

### 已完成

- 盘点已完成进度：Day 01 至 Day 10 共 40 题；剩余 60 题。
- 创建 `STUDY_PLAN_DAY11_PLUS.md`，包含进度对照、冲刺纪律、P0/P1/P2 优先级分层、Day 11 至 Day 26 每日题单、每日练法、代码白名单与五个统一模板、时间不足时的裁剪方案。
- 剩余题按高频程度分为：P0 必刷 35 题、P1 重点 22 题、P2 选刷 3 题。
- 主线安排为 Day 11 至 Day 24 覆盖全部 P0 题，Day 25 至 Day 26 补 P1/P2 和复习。
- 明确代码风格：只用通用写法，禁止海象运算符、`lru_cache`、`bisect`、位运算技巧、一行流和复杂推导式；统一二分、回溯、网格搜索、动态规划、小顶堆五个模板。
- 更新 `TODO.md`，把入门期任务归档，新增冲刺期逐日执行清单。

### 验证

- 剩余题目清单与 Hot 100 分类逐项核对一致，已完成 40 题加剩余 60 题等于 100。
- Day 11 至 Day 24 的题单逐题核对，覆盖 P0 全部 35 题。
- 本次新增与修改的都是 Markdown 文件，不需要 notebook JSON 校验。

### 下一步

从 Day 11 开始按 `STUDY_PLAN_DAY11_PLUS.md` 生成 notebook 和对应的 `dayXX_practice.ipynb`。

## 2026-09-23（生成 Day 11 链表进阶四题）

### 已完成

- 创建 `day11_linked_list_advanced.ipynb`，共 60 个单元、24 个代码单元。
- 创建练习纯享版 `day11_practice.ipynb`，共 10 个单元、4 个空练习代码格。
- 题单：两两交换链表中的节点（24）、LRU 缓存（146）、随机链表的复制（138）、排序链表（148，加练）。
- 每题前设“做题前先补”：三条指针的交换顺序、列表版 LRU 与双向链表插入删除动作、对象做字典键、快慢指针找中点与断链。
- 每道题包含解法名称、暴力与优化思路、画图跟踪、带打印的过程跟踪代码、你来写、测试代码、参考答案、面试表达。
- 两两交换额外给出递归版本；随机链表复制额外给出拼接拆分的 O(1) 空间版本；排序链表复用 Day 06 的 `merge_two_lists` 写法。
- 练习版每题为独立完整代码格，目标函数保留 `pass`，不含提示与答案。

### 验证

- 两个 notebook 均为 nbformat 4.5，通过 JSON 格式校验。
- 学习版 24 个代码格与练习版 4 个代码格可在全新命名空间顺序运行通过；练习格保持 `pass` 时测试格不报错，只提示“先完成函数”。
- 参考答案全部通过断言：两两交换 5 组边界用例，LRU 命中、淘汰、容量 1 等场景，随机链表复制通过对象身份检查与空链表检查，排序链表通过 200 组随机数据与 `sorted` 对照。
- 链表工具函数沿用 `ListNode`、`build_list`、`list_values` 与 `dummy + tail + for` 建表方法；本题新增的 `RandomNode`、`build_random_list` 只在原方法上增加 random 字段，未更换建表套路。

### 下一步

按计划继续生成 Day 12（215 第 K 大、347 前 K 高频、25 K 个一组翻转，加练 23 合并 K 个升序链表）。

## 2026-09-23（生成 Day 12 至 Day 25 全部冲刺期 notebook）

### 已完成

- 按 `STUDY_PLAN_DAY11_PLUS.md` 的题单，一次性生成 Day 12 至 Day 25 共 14 天、28 个 notebook（每天一个学习版加一个练习纯享版）。
- 新增题目 59 道，加上 Day 01 至 Day 10 的 40 道，Hot 100 已覆盖 99 道；剩余未做的是 P2 选刷题「缺失的第一个正数」（LeetCode 41）。
- 每天的学习版保持统一结构：今日主题、今天怎么学、今日题单、昨日复习、每题（做题前先补 + 最小示例、题目描述改写版、解法名称中英对照、暴力思路、优化思路、画图跟踪、过程跟踪代码、你来写、测试代码、参考答案、面试表达）、今日小结、今日复盘区。
- 练习版每题为独立完整代码格，目标函数保留 `pass`，不含提示、过程、测试与答案。

### 各天题单

- Day 12 堆与链表困难题：215、347、25，加练 23。
- Day 13 栈与设计题：394、84、295，加练 76。
- Day 14 二叉树（一）：543、199、108，加练 114。
- Day 15 二叉树（二）：105、437、124。
- Day 16 图与 Trie：200、994、207，加练 208。
- Day 17 回溯（一）：46、78、39，加练 17。
- Day 18 回溯（二）：22、79、131，加练 51。
- Day 19 贪心：121、55、45，加练 763。
- Day 20 动态规划（一）：70、118、198，加练 279。
- Day 21 动态规划（二）：322、300、139，加练 152。
- Day 22 二维 DP：5、62、64，加练 1143。
- Day 23 数组技巧（一）：136、169、75，加练 31。
- Day 24 巧解与二分收尾：287、153、48，加练 240。
- Day 25 Hard 冲刺：72、4、416，加练 32。

### 写法一致性

- 链表题沿用 `ListNode`、`build_list`、`list_values` 与 `dummy + tail + for` 建表方法；随机链表只新增 `RandomNode` 与 `build_random_list` 两个最小扩展，未更换建表套路。
- 树题沿用 Day 07、Day 08 的 `TreeNode` 与 `build_tree`；新增反向工具 `tree_values`，用于把树转回层序列表观察结果。
- 二维列表统一用 `board = []` 加 `for` 循环创建，未使用嵌套列表推导式。
- 全程只使用通用语法：无海象运算符、无 `lru_cache`、无 `bisect`、无 `itertools`（仅 136 题按要求使用异或位运算）；二分、回溯、网格搜索、动态规划、小顶堆五套模板在各天保持一致。
- 每道题的测试格在用户函数仍为 `pass` 时不报错，只提示“先把上面的函数写完再运行这一格”。

### 验证

- 28 个新文件全部为 nbformat 4.5，通过 JSON 格式校验。
- 全部代码格在全新命名空间中顺序运行通过（学习版 228 个、练习版 55 个）。
- 59 道题的参考答案全部通过断言与随机对照测试，包括：25、23、84、295、76、543、124、437、200、994、207、46、78、39、22、79、131、51、121、55、45、763、70、118、198、279、322、300、139、152、5、62、64、1143、136、169、75、31、287、153、48、240、72、4、416、32 等。
- 覆盖的对照方式：排序结果对照、暴力枚举对照、集合求子集对照、集合求排列对照、BFS 可达性对照、区间暴力对照等。

### 下一步

按 `STUDY_PLAN_DAY11_PLUS.md` 复习：每天先用 `dayXX_practice.ipynb` 重写前一天至少两道必做题；Day 26 机动日用于补齐 41 和反复重写错题。
